import styled from 'styled-components';

export const ContainerVideoRecomendedList = styled.div``;

export const EmptyVideosRecomended = styled.p`
  text-align: center;
  width: 33.4rem;
  font-size: ${({ theme }) => theme.font.sizes.small};
`;
